<% provide(:title, "Crap Update") %>

 <h1>Update your crap</h1>
 
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@crap) do |f| %>
	<%= render 'form', f: f %>
	<%= f.submit "Update my crap", class: "btn btn-primary" %>
    
    <% end %>
  </div>
</div>
 
